<article>
  <header>
    <h1><%= @movie.title %></h1>
  </header>
  <p>
    <%= @movie.description %>
  </p>
  <h3>Total Gross</h3>
  <p>
    <%= number_to_currency(@movie.total_gross) %>
  </p>
</article>

<%= link_to 'All Movies', root_path %>